The Weather App is a responsive web application built using React.js that provides real-time weather information using third-party Weather APIs. The application allows users to search for any city and view detailed weather conditions such as temperature, humidity, wind speed, and weather description.

The app fetches live weather data from external APIs and dynamically updates the UI based on user input.
